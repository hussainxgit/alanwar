class Service {
  String uid, title, employeeEntry;
  double price;

  Service(
      {required this.uid,
        required this.title,
        required this.price,
        required this.employeeEntry});
}